name: Sync live
on:
  push:
    branches: ["2026s2"]
permissions:
  contents: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: git push origin ${GITHUB_SHA}:refs/heads/live
